import 'package:flutter/material.dart';

class AndroidDistributionPage extends StatelessWidget {
  const AndroidDistributionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
